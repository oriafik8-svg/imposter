{
  "name": "imposter-functions",
  "description": "Server-side room/round logic for the imposter online mode — word, impostor, and clue selection must happen here, never on the client.",
  "engines": { "node": "20" },
  "main": "index.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  }
}
